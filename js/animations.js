(()=>{if(matchMedia('(prefers-reduced-motion: reduce)').matches){document.querySelectorAll('.reveal').forEach(el=>el.classList.add('is-visible'));return}const reveal=()=>{const elements=document.querySelectorAll('.reveal:not([data-reveal-ready])');const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}}),{threshold:.12,rootMargin:'0px 0px -40px'});elements.forEach(el=>{el.dataset.revealReady='true';observer.observe(el)})};window.addEventListener('gallery:rendered',reveal);reveal();const layers=document.querySelectorAll('.parallax-layer');if(layers.length)window.addEventListener('scroll',()=>layers.forEach(layer=>{layer.style.transform=`translateY(${scrollY*Number(layer.dataset.speed||.1)}px)`}),{passive:true})})();
